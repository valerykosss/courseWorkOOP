<UserControl x:Class="CLINICS.TimeButtonUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CLINICS"
             mc:Ignorable="d" 
             d:DesignHeight="78" d:DesignWidth="200"
             FontFamily="Montserrat">
    <Grid Width="200" Height="78">
        <Border CornerRadius="40" Cursor="Hand" BorderBrush="White" BorderThickness="1" Background="Transparent">
            <Button x:Name="timeButton" Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Click="timeButton_Click">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Time}" x:Name="time" FontWeight="Light" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" />
                    <TextBlock x:Name="timeId" Text="{Binding TimeID}" Foreground="Transparent" FontSize="0.1"></TextBlock>
                </StackPanel>
            </Button>
        </Border>
    </Grid>
</UserControl>
